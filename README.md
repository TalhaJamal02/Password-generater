# Password Generator
This project is a password generator built with React.js, utilizing both the useState and useEffect hooks. The application generates secure passwords based on user-defined criteria such as length, inclusion of special characters, numbers, and letters. It demonstrates effective state management and side effect handling in functional components.
